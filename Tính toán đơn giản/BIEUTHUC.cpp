BIEUTHUC - Tính giá trị biểu thức#include <iostream>

using namespace std;

long long a, b, c, d, x;

int main() {
    cin >> a >> b >> c >> d >> x;
    cout << a*x*x*x + b*x*x + c*x + d;
    return 0;
}
    
