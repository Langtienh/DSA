#include <iostream>

using namespace std;

long long a, b, c;

int main() {
    cin >> a >> b >> c;
    if (a + c == b + b) cout << a+b+c;
    else cout << "KHONG PHAI CAP SO CONG";
    
    return 0;
}
    
